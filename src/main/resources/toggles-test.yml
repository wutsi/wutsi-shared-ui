#wutsi:
#  toggles:
#    order: false                 # Order disabled (until we have integrated a payment provider)
#    cart: false                  # Disabled cart (until we have integrated a payment provider)
